const array = [2,4,6,8,10]

const isEven = array.every((num)=>{
  return num%2===0;
})


console.log(isEven);


//every method check whether all the elements of the array satisfy the given condition (passed by in user) or not that is provided by a method passed to it as the argument.

